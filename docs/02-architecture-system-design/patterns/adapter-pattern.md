# Adapter Pattern

Explain *why* `StorageAdapter.js` exists. Show how if the college decides to switch from Google Drive to AWS S3 next year, a new developer only needs to write an `S3Adapter.js` without touching the core `materialController.js` logic.
